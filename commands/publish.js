module.exports = () => {
    // Publish stable packages

    // Level up till we find sibling projects, look at their dependencies.

    // Update published dependencies in sibling projects.

    // Push changes in sibling projects.

    // Optionally - initiate PRs in sibling projects.
};
