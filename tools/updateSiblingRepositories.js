

module.exports = () => {
    // One step from repo (package.json location)

    // Recursively walk into each directory

    // Check whether it's a Lerna repository or a simple NPM repository.

    // If it's a simple NPM package, parse package.json to determine dependencies.

    // If it's a Lerna repository, recursively check & update child packages.
};
